# Downloaded source data. Large, and reproducible from scripts/fetch_wpp.py.
# The checksum manifest in data/manifest/ IS committed, so a fresh clone can
# verify it got byte-identical files.
data/raw/
data/interim/

# Build products that can be regenerated
data/processed/
out/
dist/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# LaTeX working files. The reviewed paper/population-model.pdf is tracked.
paper/build/
paper/tmp/
tmp/

# Tooling scratch that wanders in from elsewhere on this machine
.wrangler/

# OS / editor
Thumbs.db
.DS_Store
